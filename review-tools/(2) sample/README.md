# Sample review tool

`index.html` is the isolated sample/presentation interface.

Its durable state is `sample_all.data`. Relationship files in this folder
belong only to the sample tool. The server saves through `/api/edits-sample`.
The HTML currently inherits the original embedded corpus. Selecting and
generating a genuinely reduced sample corpus remains pending in `PROJECT_LOG.md`.
