# Formal review tool

`index.html` is the complete research review interface.

Its durable state is `formal_all.data`. Relationship files in this folder
belong only to the formal tool. The server saves through `/api/edits-formal`.
